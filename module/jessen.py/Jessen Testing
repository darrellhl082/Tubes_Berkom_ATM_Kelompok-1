# Program: Otentikasi identitas
# Spesifikasi: Mencari Username dan Password yang sesuai


# KAMUS
# Username: int; username pengguna
# Password: int; password pengguna
# i, : int
# found : bool; menentukan Username + Password sdh ditemukan/belum

# ALGORITMA
# Asumsi: input array sudah dibuat; N terdefinisi

Table_Username = [1, 2, 3, 4, 5, 6]
Table_Password = [1, 2, 3, 4, 5, 6]

Table_Username = [1, 2, 3, 4, 5, 6]
Table_Password = [1, 2, 3, 4, 5, 6]

def Input_Otentikasi():
    print("Username atau Password salah")
    print("Login ke akun masing-masing dengan menginput username dan password")
    Username = int(input("Username: "))
    Password = int(input("Password: "))
    return Username, Password

def Otentikasi(Username_Input, Password_Input):
    found = False  # found = False; X belum ditemukan
    i = 0
    while i < len(Table_Username) and not found:
        if Table_Username[i] == Username_Input and Table_Password[i] == Password_Input:
            found = True  # found = True; X sudah ditemukan
        else:
            i += 1  # hanya increment jika X belum ditemukan
    return found

# Main program
print("Login ke akun masing-masing dengan menginput username dan password")
Username = int(input("Username: "))
Password = int(input("Password: "))

# Loop until the correct username and password are entered
while not Otentikasi(Username, Password):
    Username, Password = Input_Otentikasi()

print("Username dan Password benar")